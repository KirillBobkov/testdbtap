import React from 'react';
import { CodeEditorPopupButtonStyled } from './code-editor-popup-button.styled';
export const CodeEditorPopupButton = props => (React.createElement(CodeEditorPopupButtonStyled, { ...props }));
