/**
 * Keyboard mode is a UI state when all hover elements become visible when navigating with mouse.
 * Turns ON when user press "Tab" and turns OFF on any mouse click.
 * @doc-tags a11y
 */
export declare const useKeyboardMode: () => boolean;
